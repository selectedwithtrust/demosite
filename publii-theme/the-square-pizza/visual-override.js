// Appended to the end of the theme stylesheet. Nothing to override here - all of
// this theme's visual options are handled in theme-variables.js.
// https://getpublii.com/dev/visual-overrides/
var generateOverride = function (params) {
   return '';
};

module.exports = generateOverride;
